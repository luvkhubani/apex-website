{
  "ignoreCommand": "node -e \"const m=process.env.VERCEL_GIT_COMMIT_MESSAGE||require('child_process').execSync('git log -1 --pretty=%s 2>/dev/null').toString();process.exit(m.includes('[skip ci]')?1:0)\"",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
